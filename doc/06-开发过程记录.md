# 开发过程记录

本文档用于持续记录 `mutiAgent` 项目的实际开发过程，方便中断后续接、多人协作以及回溯某项能力是在哪一轮完成的。

## 1. 文档使用说明

- 记录范围：以后端仓库 `D:\Project\ali\260409\mutiAgent` 为主，必要时补充前端仓库 `D:\Project\ali\260409\mutiAgent-Web` 的联动信息。
- 更新原则：每完成一段可验证功能，就补充“已完成 / 已验证 / 下一步”三部分。
- 阅读顺序：先看“当前状态总览”，再看“最近一次推进记录”。

## 2. 当前状态总览

截至 `2026-04-09`，当前主线已经从“基础脚手架”推进到“会话运行链路增强”阶段，重点围绕以下能力补齐：

- 会话页终端链路修复，输入不再只是单纯文本提交，而是朝 `TTY + resize` 的真实交互模式靠拢。
- 后端补齐 `PTY` 进程托管、终端尺寸调整、原始输出读取、字符集解码兜底等能力。
- 前后端已打通“会话详情页 -> 输入 -> 原始输出/终端展示 -> 状态同步”的基础闭环。
- 下一阶段主攻方向切换为“历史检索 / 全文搜索”。

## 3. 最近一次推进记录

### 3.1 本轮核心目标

围绕“让会话详情页具备更接近真实终端的交互能力”推进，优先解决以下问题：

- 原始输出链路不完整，导致页内终端体验不稳定。
- 会话启动后缺少终端尺寸同步，TUI 类程序展示异常。
- 输入记录与初始化输入混在一起，影响消息记录语义。
- 不同运行环境下存在字符集差异，输出可能出现乱码或截断。

### 3.2 本轮已完成

#### 后端

- `SessionController` 新增会话原始输出查询接口：`GET /api/v1/sessions/{id}/raw-output`
- `SessionController` 新增终端尺寸调整接口：`POST /api/v1/sessions/{id}/terminal/resize`
- `SessionAppService` 新增原始日志读取能力
- `SessionAppService` 调整输入发送逻辑，支持 `recordInput` 控制是否记录用户输入
- `SessionAppService` 新增运行中会话的终端尺寸调整逻辑
- `LocalProcessSupervisor` 引入 `pty4j` 托管，支持：
  - 在 `embedded` 或 `RAW` 交互模式下使用 `PTY`
  - 初始终端尺寸设置
  - 运行中 `resize`
  - `stdout/stderr` 与 `PTY` 输出的统一处理
  - 基于 `UTF-8 / UTF-16 / fallback charset` 的分块解码

#### 前后端联动

根据上轮会话记录，前端已同步接入以下链路：

- 会话详情页在 `RAW` 或 `embedded` 场景下走完整的页内终端链路
- 前端 `xterm` 已根据容器尺寸变化触发后端 `resize`
- 前端调用后端新增原始输出接口，补齐页内终端恢复逻辑

> 注：前端代码位于 `D:\Project\ali\260409\mutiAgent-Web`，本仓库文档仅记录联动结果，不在此处保存前端实现细节。

### 3.3 上轮已验证结果

根据上轮会话记录，以下验证已通过：

- 后端 `compileJava` 通过
- 前端 `npm.cmd run build` 通过

本轮继续开发时，如涉及历史检索相关接口或数据库结构调整，应再次补充专项验证记录。

## 4. 当前遗留事项

当前主线还没有完成的重点内容如下：

1. 历史检索接口设计
2. 全文搜索接口设计
3. 后端检索能力落地
4. 前端检索入口与结果展示
5. 检索结果与会话详情页跳转联动

## 5. 下一步开发计划

下一轮优先级建议如下：

### P1：历史检索 / 全文搜索

- 先核对现有文档里对历史检索、全文搜索、消息存储的接口约定
- 设计后端查询入参与返回结构
- 判断是直接基于现有消息表查询，还是补充 SQLite `FTS5`
- 完成会话、消息、原始输出三类数据的检索边界定义

### P2：搜索结果展示

- 在前端增加搜索入口
- 区分“会话维度命中”和“消息维度命中”
- 支持从搜索结果跳转回会话详情

### P3：行为模式补齐

- 继续梳理 `raw / embedded / external` 三种模式的职责边界
- 避免不同模式下重复走输入记录、终端恢复或状态同步逻辑

## 6. 关键代码落点

本轮能力主要落在以下文件：

- `src/main/java/com/aliano/mutiagent/controller/SessionController.java`
- `src/main/java/com/aliano/mutiagent/application/service/SessionAppService.java`
- `src/main/java/com/aliano/mutiagent/infrastructure/process/ProcessSupervisor.java`
- `src/main/java/com/aliano/mutiagent/infrastructure/process/LocalProcessSupervisor.java`
- `src/main/java/com/aliano/mutiagent/application/dto/ResizeTerminalRequest.java`
- `src/main/java/com/aliano/mutiagent/application/dto/SendInputRequest.java`

## 7. 续开发时的建议动作

如果中途中断，恢复开发时建议按下面顺序继续：

1. 先查看 `doc/06-开发过程记录.md`
2. 再查看 `git status`，确认当前未提交改动
3. 重点检查 `SessionController`、`SessionAppService`、`LocalProcessSupervisor`
4. 然后从“历史检索 / 全文搜索”主线继续实现

---

后续每完成一个阶段，直接在本文档末尾追加新的“推进记录”即可，不需要重写整份文档。
